// question number 1

let user = {}; // creates an empty object named "user"
user.name = 'John'; // adds a property "name" with value "John" to "user"
user.surname = 'Smith'; // adds a property "surname" with value "Smith" to "user"
user.name = 'Pete'; // changes the value of the "name" property to "Pete"
user.deleteProperty('name'); // deletes the "name" property from "user"

//question Number 2


function myFunction (chidi, justice, zulaikha){
    return chidi + justice + zulaikha
}
console.log(myFunction(100, 160, 130))